# Gourmand Exceptions
# Documented exceptions for files that legitimately trigger checks

# Summary litter — CLAUDE.md is a standard Claude Code project instructions file
[[exceptions]]
check = "summary_litter"
path = "CLAUDE.md"
justification = "Claude Code project instructions for AI assistant orientation"

# Primitive obsession — HTTP status codes and response size limits are standard API patterns
[[exceptions]]
check = "primitive_obsession"
path = "src/mcp_mediawiki_crunchtools/client.py"
justification = "HTTP status codes (200, 401, 403, 429) and response size limit (10MB) are standard well-known constants"

[[exceptions]]
check = "primitive_obsession"
path = "src/mcp_mediawiki_crunchtools/config.py"
justification = "Port 8016 is a standard default for HTTP servers"

[[exceptions]]
check = "primitive_obsession"
path = "src/mcp_mediawiki_crunchtools/__init__.py"
justification = "Port 8016 is a standard default for HTTP servers"

# Lint suppression — test intentionally passes invalid args to verify rejection
[[exceptions]]
check = "lint_suppression"
path = "tests/test_validation.py"
justification = "type: ignore[call-arg] is required to test that Pydantic rejects extra fields"

# Implicit state machine — optional parameter builders are not state machines
[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/tools/pages.py"
justification = "Optional API parameter builders use simple if-guards to add query params"

[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/tools/categories.py"
justification = "Optional API parameter builders use simple if-guards to add query params"

[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/tools/recent_changes.py"
justification = "Optional API parameter builders use simple if-guards to add query params"

[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/tools/users.py"
justification = "Optional API parameter builders use simple if-guards to add query params"

[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/tools/files.py"
justification = "Optional API parameter builders use simple if-guards to add query params"

[[exceptions]]
check = "implicit_state_machine"
path = "src/mcp_mediawiki_crunchtools/client.py"
justification = "Error handling dispatches on MediaWiki error codes — standard pattern, not a state machine"
